# Minimal environment variables (loaded by all zsh invocations)
export EDITOR="nvim"
export VISUAL="nvim"
export XDG_CONFIG_HOME="$HOME/.config"
export APPLE_SSH_ADD_BEHAVIOR

# Keep PATH minimal here; full PATH setup lives in .zprofile
export PATH="$HOME/.local/bin:$HOME/bin:$PATH"

